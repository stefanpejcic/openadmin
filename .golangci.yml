version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - ineffassign
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
